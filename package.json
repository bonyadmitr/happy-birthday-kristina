{
  "name": "happy-birthday-kristina",
  "version": "1.0.0",
  "private": true,
  "description": "Dev tooling only. Сам сайт — статика (index.html, css, js), без сборки.",
  "scripts": {
    "test": "playwright test",
    "test:report": "playwright show-report",
    "icons": "node scripts/gen-icons.mjs",
    "serve": "python3 -m http.server 8123"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
